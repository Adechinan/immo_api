<div>
    <label class="form-label" for="{{ $name }}"> {{ ucfirst($label) }} </label>
    <textarea class="form-control" name="{{ $name }}" id="{{ $name }}" cols="30" rows="10">{{ $value }}</textarea>
</div>